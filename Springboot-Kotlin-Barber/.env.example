# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=barber
DB_USERNAME=postgres
DB_PASSWORD=changeme

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=changeme-this-should-be-a-very-long-secure-secret-key-at-least-256-bits-for-production

# Application Configuration
SPRING_PROFILE=dev

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate Limiting
RATE_LIMIT_GUEST_RPM=20
RATE_LIMIT_REGISTERED_RPM=100

# Server Configuration
SERVER_PORT=8080
